[workspace]
resolver = "2"
members = ["crates/determinism", "crates/sim", "crates/harness", "crates/net"]

[workspace.package]
edition = "2021"
license = "MIT"

[profile.release]
overflow-checks = true   # determinism: never silently wrap in release
