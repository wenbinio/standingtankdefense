[package]
name = "preview"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[[bin]]
name = "preview"
path = "src/main.rs"

[dependencies]
sim = { path = "../sim" }
determinism = { path = "../determinism" }
